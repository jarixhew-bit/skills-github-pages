# 已确认可以公开的字面值，一行一个，check-secrets.py 扫描时跳过含这些值的行。
# 登记前必须写清楚"为什么公开也没事"，不是"看起来像误报就抄进来"。

# Firebase Web SDK 的 apiKey（expense-tracker.html），只是项目标识符不是传统
# 密钥；Google 官方文档明确这类客户端 key 设计上就是公开的，真正的访问控制由
# Firestore/Auth 安全规则负责（该项目规则已于 2026-07-16 改成 users/{uid} 限权，
# 见 .claude/rules/diagnosis.md 教训纪录）。
AIzaSyCfZ407K6xNTvIpqKoFa_lkULgmrZwKJBM

# expense-tracker-opencv.js:30 里的 "AKIARKBEADQCAIIANBAX" 是误报——这是官方
# OpenCV.js（Emscripten 编译产物，vendored 进本仓库供同源加载，见
# expense-tracker-opencv.LICENSE.txt）压缩后运行时代码里偶然出现的字符片段，
# 上下文是普通的模块初始化样板代码，跟 AWS 或任何密钥无关。
AKIARKBEADQCAIIANBAX

# vendor/tesseract/tesseract-core*.wasm.js 里的这两串是误报：它们出现在一行
# 460 万字符的 base64 编码 WebAssembly 二进制数据当中（约第 256 万字符处），
# 前后都是编译产物的 base64 片段，不是任何凭证。这些文件是官方 tesseract.js
# 5.1.1 的 wasm 运行时，2026-08-02 vendored 进本仓库供白名单 WiFi 下同源加载
# （见 .claude/notes/expense-tracker.md 该日条目）。四个变体里出现两种拼法。
# 检验方法：AWS access key ID 是 AKIA 开头的 20 字符，在 460 万字符的 base64
# 里偶然撞出这个形状是大概率事件，不是泄漏。
AKIAUYEQCADLQCOAQ0CC
AKIAUYEQCAGLQCOAQ0CC

# Google Maps JS API key，曾写在 japan-trip-2026.html / tokyo-itinerary.html 里
# （前端 Maps key 本来就会随网页发给浏览器，无法保密，只能靠引用来源限制＋用量上限）。
# 2026-08-05 实测已失效：调 Geocoding API 返回
#   REQUEST_DENIED / "This API project was not found. This API project may have been deleted"
# ——它所属的整个 Google Cloud 项目已删除，key 无法再调用任何 API，也不会产生账单。
# 页面上早已不再引用它，但仍留在 git 历史里（公开仓库删不掉），故登记于此避免月度扫描重复报。
# 想复验：curl -s "https://maps.googleapis.com/maps/api/geocode/json?address=Tokyo&key=<该key>"
AIzaSyCdPSJBGOPzM4v-MyX9ncfLqjMM4Du_Pbc
